# Backend
PORT=8080
HOST=0.0.0.0
DEBUG=false
MODEL_DIR=../model

# Frontend (used at build time by Vite)
# Leave empty for same-origin (Docker/nginx proxy), or set to full URL for external API
VITE_API_URL=
